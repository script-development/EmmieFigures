import {defineConfig} from 'windicss/helpers';

export default defineConfig({
    theme: {
        extend: {
            fontFamily: {
                junegull: ['Junegull', 'monospace'],
                poppins: ['poppins', 'sans-serif'],
            },
        },
    },
});
